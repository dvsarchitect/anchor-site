/* =============================================================
   REFINED NEUTRAL - COMPLETE OVERRIDE (Option 1 Approved)
   Neutral grays + navy accent ONLY. Overrides all base styles.
   ============================================================= */

/* GLOBAL PALETTE - Force override existing vars */
:root {
  /* Neutral scale (grays only) */
  --neutral-0: #ffffff !important;
  --neutral-50: #f9f9f9 !important;
  --neutral-100: #f0f0f0 !important;
  --neutral-200: #e2e2e2 !important;
  --neutral-300: #c8c8c8 !important;
  --neutral-500: #7a7a7a !important;
  --neutral-600: #555555 !important;
  --neutral-700: #3a3a3a !important;
  --neutral-800: #222222 !important;
  --neutral-900: #111111 !important;

  /* Accent (ONLY place navy appears) */
  --accent-navy: #1f3a5f !important;
  --accent-navy-hover: #152a43 !important;

  /* Override ALL site color variables to neutrals */
  --color-primary: #222222 !important;        /* Headings - dark gray */
  --color-secondary: #555555 !important;      /* Subheads - mid gray */
  --color-text: #3a3a3a !important;           /* Body text - gray */
  --color-text-light: #7a7a7a !important;     /* Muted - light gray */
  --color-bg: #ffffff !important;             /* Page bg - white */
  --color-bg-alt: #f5f5f5 !important;         /* Alt sections - lightest gray */
  --color-border: #e2e2e2 !important;         /* Borders - subtle gray */
  
  /* Accents (navy for links/CTAs, warm for Monday) */
  --color-accent: #1f3a5f !important;         /* Links/buttons - NAVY */
  --color-accent-light: #2a4e7a !important;   /* Hover - lighter navy */
  --color-monday: #d97706 !important;         /* Monday badge - warm orange */
  --color-friday: #1f3a5f !important;         /* Friday badge - navy */

  /* Shadows */
  --shadow-sm: 0 1px 2px rgba(0,0,0,0.06) !important;
  --shadow-md: 0 4px 14px rgba(0,0,0,0.08) !important;
  --shadow-lg: 0 10px 32px rgba(0,0,0,0.12) !important;
  
  /* Motion */
  --ease: cubic-bezier(.22,.61,.36,1) !important;
}

[data-theme="dark"] {
  --color-primary: #f1f1f1 !important;
  --color-secondary: #c8c8c8 !important;
  --color-text: #e6e6e6 !important;
  --color-text-light: #aeadad !important;
  --color-bg: #0f1115 !important;
  --color-bg-alt: #1a1f26 !important;  /* Increased contrast */
  --color-border: #2a2e36 !important;
  --color-accent: #7aa0e8 !important;
  --color-accent-light: #9dbaf1 !important;
  --color-friday: #7aa0e8 !important;
  --shadow-sm: 0 1px 2px rgba(0,0,0,0.4) !important;
  --shadow-md: 0 4px 16px rgba(0,0,0,0.5) !important;
  --shadow-lg: 0 12px 40px rgba(0,0,0,0.55) !important;
}

[data-theme="dark"] .hero-card,
[data-theme="dark"] .card {
  border: 1px solid var(--color-border) !important;
  box-shadow: 0 2px 8px rgba(0,0,0,0.3) !important;
}

/* ========== NAVBAR SPECIFIC OVERRIDES ========== */
/* Force navbar to use neutral grays, not blues */
.navbar {
  background: rgba(255,255,255,.95) !important;
  border-bottom: 1px solid #e2e2e2 !important;
  position: sticky !important;
  top: 0 !important;
  z-index: 1000 !important;
  padding: 0 !important;
}

.navbar .container {
  padding: 0 2rem !important;
}

.nav-content {
  max-width: 1320px !important;
  margin: 0 auto !important;
  padding: 0 !important;
  display: flex !important;
  align-items: center !important;
  height: 70px !important;
  gap: 3rem !important;
}

/* Override featured-images.css global lazy-load rule */
img[loading="lazy"] {
  opacity: 1 !important; /* Force visible - script.js will handle fade-in */
}

.logo {
  color: #222222 !important;
  display: flex !important;
  align-items: center !important;
  gap: .625rem !important;
  font-family: 'Playfair Display', Georgia, serif !important;
  font-size: 1.375rem !important;
  font-weight: 700 !important;
  text-decoration: none !important;
  flex-shrink: 0 !important;
}

.logo img {
  display: block !important;
  width: 40px !important;
  height: 40px !important;
  border-radius: 10px !important;
  opacity: 1 !important;
}

.logo span {
  display: inline !important;
  color: inherit !important;
}

.nav-links {
  display: flex !important;
  align-items: center !important;
  gap: 2rem !important;
  flex: 1 !important;
}

.nav-links a {
  color: #3a3a3a !important;
  font-size: .9375rem !important;
  font-weight: 500 !important;
  text-decoration: none !important;
  transition: color .2s ease !important;
  white-space: nowrap !important;
}

.nav-links a:hover {
  color: #1f3a5f !important; /* Navy accent on hover ONLY */
}

[data-theme="dark"] .navbar {
  background: rgba(15,17,21,.95) !important;
  border-bottom: 1px solid #2a2e36 !important;
}

[data-theme="dark"] .logo {
  color: #f1f1f1 !important;
}

[data-theme="dark"] .logo span {
  color: #f1f1f1 !important;
}

[data-theme="dark"] .nav-links a {
  color: #e6e6e6 !important;
}

[data-theme="dark"] .nav-links a:hover {
  color: #7aa0e8 !important;
}

.theme-toggle {
  margin-left: auto !important;
  background: transparent !important;
  border: 1px solid var(--color-border) !important;
  color: var(--color-text) !important;
  border-radius: .5rem !important;
  padding: .5rem .7rem !important;
  cursor: pointer !important;
  display: inline-flex !important;
  align-items: center !important;
  justify-content: center !important;
  gap: .5rem !important;
  transition: background .2s ease !important;
  flex-shrink: 0 !important;
}

.theme-toggle:hover {
  background: var(--color-bg-alt) !important;
}

/* Typography - Playfair for headings, Inter for body */
body{font-family:Inter,-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,sans-serif;background:var(--color-bg);color:var(--color-text);} 
h1,h2,h3,h4,h5,h6{font-family:'Playfair Display',Georgia,serif;color:var(--color-primary);line-height:1.15;margin:0 0 .75rem}
.hero .lead{font-family:'Playfair Display',Georgia,serif;color:var(--color-primary);}

/* Container - needed for navbar and layout */
.container{max-width:1320px;margin:0 auto;padding:0 2rem}

/* ========== FEATURED HERO ========== */
.hero-featured{padding:4rem 0 3rem;background:var(--color-bg)}
.featured-content{display:grid;grid-template-columns:1fr 1fr;gap:4rem;align-items:center}
.featured-text .eyebrow{font-size:.85rem;text-transform:uppercase;letter-spacing:.12em;font-weight:600;color:var(--color-accent);margin-bottom:1rem}
.featured-text h1{font-size:clamp(2rem,1.5rem + 2vw,3.2rem);margin-bottom:1.25rem;line-height:1.1;color:var(--color-primary)}
.featured-text .lead{font-size:clamp(1.1rem,1rem + .3vw,1.35rem);line-height:1.6;max-width:48ch;margin-bottom:1.5rem;color:var(--color-text);font-family:Inter,sans-serif}
.featured-meta{display:flex;gap:1rem;flex-wrap:wrap;align-items:center;margin-bottom:2rem;font-size:.9rem;color:var(--color-text-light)}
.featured-meta .badge{background:var(--color-accent);color:#fff;padding:.35rem .75rem;border-radius:6px;font-weight:600;font-size:.85rem}
.featured-image{position:relative;border-radius:16px;overflow:hidden;aspect-ratio:16/10;box-shadow:var(--shadow-lg)}
.featured-image img{width:100%;height:100%;object-fit:cover;display:block;opacity:1 !important}

@media(max-width:900px){
  .featured-content{grid-template-columns:1fr;gap:2rem}
  .featured-image{aspect-ratio:16/9}
}

/* Buttons */
.primary-actions{display:flex;gap:1.1rem;flex-wrap:wrap}
.btn{cursor:pointer;font-weight:500;font-size:.95rem;border:none;padding:.95rem 1.6rem;border-radius:8px;display:inline-flex;align-items:center;gap:.5rem;transition:background .3s,color .3s,box-shadow .3s;text-decoration:none}
.btn-primary{background:var(--color-accent);color:#fff !important;box-shadow:var(--shadow-sm)}
.btn-primary:hover{background:var(--color-accent-light);box-shadow:var(--shadow-md)}
.btn-outline{background:transparent;color:var(--color-primary);border:1px solid var(--color-border)}
.btn-outline:hover{background:var(--color-bg-alt)}

[data-theme="dark"] .btn-primary {
  color: #ffffff !important;
  background: var(--color-accent) !important;
}

[data-theme="dark"] .btn-outline {
  color: var(--color-primary) !important;
  border-color: var(--color-border) !important;
}

.section-divider{border:none;height:1px;background:var(--color-border);margin:4.5rem 0 3rem}

/* ========== ABOUT SECTION ========== */
.about-section{padding:4rem 0;background:var(--color-bg-alt)}
.about-refined{padding:0}
.about-content{display:grid;grid-template-columns:1.3fr 1fr;gap:4rem;align-items:start}
.about-text{max-width:680px}
.about-text h2{color:var(--color-primary);font-size:clamp(2rem,1.8rem + .6vw,2.5rem);margin-bottom:1rem}
.about-text .lead{font-size:1.25rem;color:var(--color-secondary);margin-bottom:1.5rem;font-weight:500}
.about-text p{font-size:1.05rem;line-height:1.7;color:var(--color-text);margin-bottom:1.25rem}
.about-cta{margin-top:2.5rem;padding-top:2.5rem;border-top:1px solid var(--color-border)}
.about-cta h3{font-size:1.35rem;color:var(--color-primary);margin-bottom:1.25rem}
.social-links-about{display:flex;gap:1rem;flex-wrap:wrap}
.social-link{display:inline-flex;align-items:center;gap:.65rem;padding:.85rem 1.35rem;background:var(--color-bg);border:1px solid var(--color-border);border-radius:8px;color:var(--color-text);text-decoration:none;font-weight:500;font-size:.95rem;transition:all .3s var(--ease)}
.social-link:hover{transform:translateY(-2px);box-shadow:var(--shadow-md);border-color:var(--color-accent)}
.social-link.substack:hover{background:#ff6719;color:#fff;border-color:#ff6719}
.social-link.twitter:hover{background:#000;color:#fff;border-color:#000}
.social-link.facebook:hover{background:#1877f2;color:#fff;border-color:#1877f2}
.social-link svg{width:20px;height:20px;flex-shrink:0}
[data-theme="dark"] .social-link{background:var(--color-border);border-color:var(--neutral-600)}
[data-theme="dark"] .social-link:hover{background:var(--color-accent);color:var(--color-bg);border-color:var(--color-accent)}
.about-image{position:relative}
.about-logo-display{width:100%;max-width:300px;margin:0 auto 2.5rem;padding:2rem;background:var(--color-bg);border:1px solid var(--color-border);border-radius:16px;box-shadow:var(--shadow-md)}
.about-logo-display img{width:100%;height:auto;display:block;border-radius:12px}
.about-stats{display:grid;grid-template-columns:repeat(3,1fr);gap:1.5rem}
.stat-item{text-align:center;padding:1.5rem 1rem;background:var(--color-bg);border:1px solid var(--color-border);border-radius:12px}
.stat-number{font-size:2.25rem;font-weight:700;color:var(--color-accent);margin-bottom:.35rem;font-family:'Playfair Display',serif}
.stat-label{font-size:.85rem;color:var(--color-text-light);text-transform:uppercase;letter-spacing:.08em;font-weight:600}
@media(max-width:900px){
  .about-content{grid-template-columns:1fr;gap:3rem}
  .about-text{max-width:100%}
  .about-logo-display{max-width:240px}
  .about-stats{gap:1rem}
}

/* ========== WHAT I'M WATCHING SECTION ========== */
.watching-section{padding:4rem 0;background:var(--color-bg)}
.watching-content{max-width:900px;margin:0 auto}
.section-head-centered{text-align:center;margin-bottom:2.5rem}
.section-head-centered h2{color:var(--color-primary);font-size:clamp(2rem,1.8rem + .6vw,2.5rem);margin-bottom:.75rem}
.section-head-centered .section-subtitle{color:var(--color-text);font-size:1.1rem;margin:0}
@media(max-width:900px){
  .watching-section{padding:3rem 0}
  .section-head-centered{margin-bottom:2rem}
}

/* ========== SUBSCRIBE SECTION ========== */
.subscribe-section{padding:3rem 0 4rem;background:var(--color-bg)}
.subscribe-refined{padding:0}
.subscribe-inner{background:var(--color-bg-alt);border:1px solid var(--color-border);border-radius:16px;padding:3rem;display:grid;grid-template-columns:1fr 1.2fr;gap:3rem;align-items:center}
.subscribe-text h2{margin:0 0 .75rem;color:var(--color-primary);font-size:clamp(1.75rem,1.5rem + .5vw,2.25rem)}
.subscribe-text .sub-text{color:var(--color-text);font-size:1.05rem;margin:0}
.subscribe-form-wrap{display:flex;justify-content:flex-end}
.embed-wrap{display:flex;justify-content:center}
.embed-wrap iframe{max-width:100%;border-radius:8px !important;box-shadow:0 2px 8px rgba(0,0,0,.1) !important}
.subscribe-form{display:flex;gap:.75rem;width:100%}
.subscribe-form input{flex:1;padding:.95rem 1.25rem;border:1px solid var(--color-border);border-radius:8px;background:var(--color-bg);color:var(--color-text);font-size:.95rem}
@media(max-width:900px){
  .subscribe-inner{grid-template-columns:1fr;gap:2rem;padding:2rem}
  .subscribe-form-wrap{justify-content:center}
}

/* ========== CARDS GRID (Recent Devotionals) ========== */
.devotionals-refined{padding:3rem 0;background:var(--color-bg)}
.section-head{display:flex;align-items:baseline;justify-content:space-between;margin-bottom:2rem}
.section-head h2{color:var(--color-primary)}
.section-head .hint{font-size:.95rem;color:var(--color-text-light)}
.grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(320px,1fr));gap:2.6rem}

/* Card component */
.card{background:var(--color-bg-alt);border:1px solid var(--color-border);border-radius:14px;overflow:hidden;display:flex;flex-direction:column;transition:transform .4s var(--ease),box-shadow .4s var(--ease)}
.card:hover{transform:translateY(-8px);box-shadow:var(--shadow-lg)}
.card-thumb{position:relative;height:260px;width:100%;background:var(--neutral-100);overflow:hidden}
.card-thumb img{width:100%;height:100%;object-fit:cover;display:block !important;opacity:1 !important;transition:opacity .3s ease}
.card-body{padding:1.6rem;display:flex;flex-direction:column;flex:1}
.card-meta{display:flex;align-items:center;gap:.75rem;font-size:.8rem;font-weight:600;letter-spacing:.08em;color:var(--color-text-light);text-transform:uppercase;margin-bottom:.9rem}
.badge{background:var(--neutral-100);padding:.25rem .6rem;border-radius:4px;color:var(--color-text);font-weight:600}
[data-theme="dark"] .badge{background:var(--color-border);color:var(--color-text)}
.card h3{font-size:clamp(1.1rem,1rem + .3vw,1.35rem);margin-bottom:.55rem;color:var(--color-primary)}
.card p{font-size:.98rem;flex-grow:1;margin-bottom:1.2rem;color:var(--color-text)}
.card a.more{font-size:.95rem;font-weight:500;color:var(--color-accent);text-decoration:none}
.card a.more:hover{color:var(--color-accent-light)}

/* Hero card images */
.hero-card img{height:220px;width:100%;object-fit:cover;display:block !important;opacity:1 !important;transition:opacity .3s ease, transform .6s var(--ease)}
.hero-card:hover img{transform:scale(1.03)}

/* ========== DEVOTIONALS GRID ========== */
.devotionals-refined{padding:4rem 0}
.reveal{opacity:0;transform:translateY(18px);transition:opacity .6s var(--ease),transform .6s var(--ease)}
.reveal.is-visible{opacity:1;transform:translateY(0)}

/* Responsive */
@media(max-width:1100px){.hero-text{grid-column:span 12;margin-bottom:3rem}.hero-aside{grid-column:span 12;grid-template-columns:repeat(12,1fr)}.hero-card{grid-column:span 3}}
@media(max-width:900px){.hero-card{grid-column:span 4}}
@media(max-width:760px){.hero-card{grid-column:span 6}.section-head{flex-direction:column;align-items:flex-start;gap:.75rem}}
@media(max-width:560px){.hero-card{grid-column:span 12}.hero-text h1{font-size:clamp(1.8rem,1.5rem + 2vw,2.4rem)}}

/* ========== YOUTUBE EMBED ========== */
.youtube-embed{position:relative;width:100%;max-width:800px;margin:2rem auto;padding-bottom:56.25%;height:0;overflow:hidden;border-radius:12px;box-shadow:var(--shadow-lg);background:var(--neutral-900)}
.youtube-embed iframe{position:absolute;top:0;left:0;width:100%;height:100%;border:0;border-radius:12px}
[data-theme="dark"] .youtube-embed{box-shadow:0 10px 40px rgba(0,0,0,.4)}
@media(max-width:900px){
  .youtube-embed{max-width:100%;margin:1.5rem 0;border-radius:8px}
  .youtube-embed iframe{border-radius:8px}
}

/* ========== FOOTER ========== */
.footer{background:var(--neutral-50);border-top:1px solid var(--neutral-200);padding:3rem 0 1.5rem;margin-top:4rem}
.footer-content{display:grid;grid-template-columns:1.5fr 2fr;gap:3rem;margin-bottom:2.5rem;align-items:start}
.footer-logo{font-family:var(--font-serif);font-size:1.5rem;font-weight:700;color:var(--neutral-900);margin-bottom:.5rem}
.footer-tagline{font-size:.9375rem;color:var(--neutral-600);margin:0}
.footer-links{display:grid;grid-template-columns:repeat(3,1fr);gap:2rem}
.footer-column h4{font-family:var(--font-sans);font-size:.875rem;font-weight:600;text-transform:uppercase;letter-spacing:.05em;color:var(--neutral-900);margin-bottom:1rem}
.footer-column a{display:block;font-size:.9375rem;color:var(--neutral-600);margin-bottom:.625rem;transition:color .2s ease}
.footer-column a:hover{color:var(--neutral-900)}
.footer-bottom{display:flex;justify-content:space-between;align-items:center;padding-top:2rem;border-top:1px solid var(--neutral-200);font-size:.875rem;color:var(--neutral-500)}
.footer-bottom a{color:var(--neutral-600);transition:color .2s ease}
.footer-bottom a:hover{color:var(--neutral-900)}

/* Footer Dark Mode */
[data-theme="dark"] .footer{background:var(--neutral-900);border-color:var(--neutral-800)}
[data-theme="dark"] .footer-logo{color:var(--neutral-50)}
[data-theme="dark"] .footer-tagline{color:var(--neutral-400)}
[data-theme="dark"] .footer-column h4{color:var(--neutral-200)}
[data-theme="dark"] .footer-column a{color:var(--neutral-400)}
[data-theme="dark"] .footer-column a:hover{color:var(--neutral-50)}
[data-theme="dark"] .footer-bottom{border-color:var(--neutral-800);color:var(--neutral-500)}
[data-theme="dark"] .footer-bottom a{color:var(--neutral-400)}
[data-theme="dark"] .footer-bottom a:hover{color:var(--neutral-50)}

/* Footer Responsive */
@media(max-width:900px){
  .footer-content{grid-template-columns:1fr;gap:2rem}
  .footer-links{gap:1.5rem}
}
@media(max-width:560px){
  .footer-links{grid-template-columns:1fr;gap:1.5rem}
  .footer-bottom{flex-direction:column;gap:.75rem;text-align:center}
}

/* ========== DEVOTIONAL SINGLE PAGE ========== */
.devotional-article{padding:0}
.article-header{background:var(--color-bg-alt);padding:3rem 0 2rem;border-bottom:1px solid var(--color-border)}
.container-narrow{max-width:820px;margin:0 auto;padding:0 2rem}
.article-label{display:inline-block;font-size:.8rem;font-weight:600;text-transform:uppercase;letter-spacing:.08em;padding:.4rem .9rem;border-radius:6px;margin-bottom:1rem}
.monday-label{background:var(--neutral-100);color:var(--neutral-700)}
.friday-label{background:var(--neutral-100);color:var(--neutral-700)}
.article-title{font-family:var(--font-serif);font-size:2.75rem;line-height:1.2;color:var(--color-primary);margin-bottom:1rem}
.article-meta{display:flex;flex-wrap:wrap;align-items:center;gap:.5rem;font-size:.9rem;color:var(--color-text-light)}
.article-meta .separator{margin:0 .25rem}
.scripture-ref{font-weight:600;color:var(--color-accent)}
.article-hero-image{margin:2.5rem 0;border-radius:12px;overflow:hidden;box-shadow:var(--shadow-md)}
.article-hero-image img{width:100%;height:auto;display:block}
.ratio-21-9{aspect-ratio:21/9}
.article-body{padding:2rem 0;font-size:1.125rem;line-height:1.8;color:var(--color-text)}
.article-body h2{font-family:var(--font-serif);font-size:1.875rem;margin:2.5rem 0 1.25rem;color:var(--color-primary)}
.article-body h3{font-family:var(--font-serif);font-size:1.5rem;margin:2rem 0 1rem;color:var(--color-primary)}
.article-body p{margin-bottom:1.5rem}
.article-body ul,.article-body ol{margin:1.5rem 0;padding-left:1.75rem}
.article-body li{margin-bottom:.75rem}
.article-body blockquote{border-left:4px solid var(--color-accent);padding-left:1.5rem;margin:2rem 0;font-style:italic;color:var(--color-secondary)}
.article-body a{color:var(--color-accent);text-decoration:underline;transition:color .2s ease}
.article-body a:hover{color:var(--color-accent-hover)}
.article-body code{background:var(--neutral-100);padding:.2rem .4rem;border-radius:4px;font-size:.95em;font-family:monospace}
.article-body pre{background:var(--neutral-100);padding:1.25rem;border-radius:8px;overflow-x:auto;margin:1.5rem 0}
.article-body pre code{background:none;padding:0}
.article-footer{border-top:1px solid var(--color-border);padding-top:3rem;margin-top:3rem;margin-bottom:4rem}
.author-card{background:var(--color-bg-alt);border:1px solid var(--color-border);border-radius:12px;padding:2rem;margin-bottom:2.5rem}
.author-name{font-family:var(--font-serif);font-size:1.5rem;font-weight:700;color:var(--color-primary);margin-bottom:.75rem}
.author-bio{font-size:1.05rem;line-height:1.7;color:var(--color-secondary);margin:0}
.share-section h4{font-family:var(--font-serif);font-size:1.25rem;color:var(--color-primary);margin-bottom:1rem}
.share-buttons{display:flex;gap:.75rem;flex-wrap:wrap}
.share-btn{background:var(--color-bg-alt);border:1px solid var(--color-border);color:var(--color-text);padding:.85rem 1.35rem;border-radius:8px;font-size:.95rem;font-weight:500;cursor:pointer;transition:all .3s ease}
.share-btn:hover{background:var(--color-accent);color:#fff;border-color:var(--color-accent)}

/* Article Dark Mode */
[data-theme="dark"] .article-header{background:var(--color-bg)}
[data-theme="dark"] .monday-label,[data-theme="dark"] .friday-label{background:var(--neutral-800);color:var(--neutral-300)}
[data-theme="dark"] .article-body code{background:var(--neutral-800)}
[data-theme="dark"] .article-body pre{background:var(--neutral-800)}
[data-theme="dark"] .author-card{background:var(--color-bg);border-color:var(--neutral-800)}
[data-theme="dark"] .share-btn{background:var(--color-bg);border-color:var(--neutral-800)}
[data-theme="dark"] .share-btn:hover{background:var(--color-accent);border-color:var(--color-accent)}

/* Article Responsive */
@media(max-width:760px){
  .article-title{font-size:2rem}
  .article-body{font-size:1.0625rem}
  .article-hero-image{margin:1.5rem 0}
  .ratio-21-9{aspect-ratio:16/9}
}

/* ========== PAGINATION ========== */
.pagination{display:flex;justify-content:center;align-items:center;gap:1rem;margin-top:4rem;padding-top:3rem;border-top:1px solid var(--color-border)}
.pagination a,.pagination span{padding:.75rem 1.25rem;border:1px solid var(--color-border);border-radius:8px;color:var(--color-text);text-decoration:none;font-weight:500;transition:all .3s ease}
.pagination a:hover{background:var(--color-accent);color:#fff;border-color:var(--color-accent)}
.pagination .active{background:var(--color-accent);color:#fff;border-color:var(--color-accent)}
.pagination .disabled{opacity:.4;cursor:not-allowed}
[data-theme="dark"] .pagination a,[data-theme="dark"] .pagination span{border-color:var(--neutral-800)}

/* ========== UTILITY CLASSES ========== */
.hint{font-size:.9rem;color:var(--color-text-light)}
.more{color:var(--color-accent);font-weight:500;text-decoration:none;font-size:.95rem;transition:color .2s ease}
.more:hover{color:var(--color-accent-hover)}
.separator{color:var(--color-text-light);margin:0 .25rem}
.eyebrow{font-size:.85rem;font-weight:600;text-transform:uppercase;letter-spacing:.08em;color:var(--color-accent);margin-bottom:.75rem}
.lead{font-size:1.25rem;line-height:1.6;color:var(--color-secondary)}
.section-subtitle{font-size:1.05rem;color:var(--color-secondary);margin-top:.5rem}

/* ========== MOBILE MENU ========== */
.mobile-menu-toggle{display:none;flex-direction:column;gap:4px;background:none;border:none;cursor:pointer;padding:.5rem;flex-shrink:0}
.mobile-menu-toggle span{width:24px;height:2px;background:var(--color-primary);transition:all .3s ease;border-radius:2px}
[data-theme="dark"] .mobile-menu-toggle span{background:var(--color-text)}

@media(max-width:900px){
  .nav-content{gap:1.5rem}
  .nav-links{gap:1.5rem}
}

@media(max-width:760px){
  .nav-content{height:60px;gap:1rem;padding:0 !important}
  .navbar .container{padding:0 1.25rem !important}
  .container{padding:0 1.25rem}
  .container-narrow{padding:0 1.25rem}
  
  .nav-links{display:none}
  .mobile-menu-toggle{display:flex}
  
  /* Mobile menu open state - if you add JS for this */
  .nav-links.mobile-open{
    display:flex;
    flex-direction:column;
    position:absolute;
    top:60px;
    left:0;
    right:0;
    background:var(--color-bg);
    border-bottom:1px solid var(--color-border);
    padding:1.5rem;
    gap:1rem;
    box-shadow:var(--shadow-lg)
  }
  [data-theme="dark"] .nav-links.mobile-open{
    background:var(--color-bg);
    border-color:var(--color-border)
  }
  
  .logo{font-size:1.125rem}
  .logo img{width:32px;height:32px}
  .theme-toggle{padding:.4rem .5rem}
  
  /* Hero responsive */
  .hero-featured{padding:2.5rem 0 2rem}
  .featured-content{gap:2rem}
  .featured-text h1{font-size:1.75rem}
  .featured-text .lead{font-size:1.05rem}
  .featured-meta{font-size:.85rem;gap:.75rem}
  .primary-actions{gap:.85rem}
  .btn{padding:.8rem 1.3rem;font-size:.9rem}
  
  /* Cards responsive */
  .grid{grid-template-columns:1fr;gap:2rem}
  .card-thumb{height:220px}
  .card-body{padding:1.35rem}
  
  /* About responsive */
  .about-section{padding:2.5rem 0}
  .about-text h2{font-size:1.75rem}
  .about-text .lead{font-size:1.1rem}
  .about-text p{font-size:1rem}
  .about-stats{grid-template-columns:repeat(3,1fr);gap:1rem}
  .stat-item{padding:1.25rem .75rem}
  .stat-number{font-size:1.75rem}
  .stat-label{font-size:.75rem}
  .social-link{padding:.75rem 1.1rem;font-size:.9rem}
  
  /* Watching responsive */
  .watching-section{padding:2.5rem 0}
  
  /* Subscribe responsive */
  .subscribe-section{padding:2.5rem 0 3rem}
  .subscribe-inner{padding:2rem 1.5rem}
  .subscribe-text h2{font-size:1.5rem}
  .subscribe-text .sub-text{font-size:1rem}
  
  /* Article responsive */
  .article-header{padding:2rem 0 1.5rem}
  .article-title{font-size:1.75rem}
  .article-body{font-size:1.05rem;padding:1.5rem 0}
  .article-body h2{font-size:1.5rem;margin:2rem 0 1rem}
  .article-body h3{font-size:1.25rem}
  .author-card{padding:1.5rem}
  .author-name{font-size:1.25rem}
  .author-bio{font-size:1rem}
  .share-buttons{flex-direction:column}
  .share-btn{width:100%;justify-content:center;text-align:center}
}

@media(max-width:560px){
  .nav-content{height:56px}
  .logo{font-size:1rem}
  .logo img{width:28px;height:28px}
  
  .featured-text h1{font-size:1.5rem}
  .featured-text .lead{font-size:1rem}
  .btn{padding:.75rem 1.1rem;font-size:.875rem}
  
  .about-stats{grid-template-columns:1fr;gap:.75rem}
  .stat-item{padding:1rem}
  .social-links-about{flex-direction:column}
  .social-link{width:100%;justify-content:center}
  
  .subscribe-inner{padding:1.5rem 1.25rem}
  .subscribe-text h2{font-size:1.35rem}
  
  .article-title{font-size:1.5rem}
  .article-body{font-size:1rem}
  .article-body h2{font-size:1.35rem}
}

